---
generated_by: Claude Opus 4.7
date: 2026-04-18
type: design
project: ai-to-web
---

# TUI 基盤 設計書（確定版）

> Generated by Claude Opus 4.7 — 2026年4月18日
>
> 初版は同日中に原資料 L3339–L5131 の再精読を経て確定版に改訂。

## 背景

`02-next-steps-plan.md` Phase 1 トラック2。原資料 `C言語と現代技術.md` L3339–L5131 の再精読により、ペイン名・レイアウト比率・型定義・色分け・操作キーが確定した（精読結果は git 履歴外の作業メモ。重要な決定は本書に集約）。

## 画面レイアウト（確定）

```
┌ Target / Source ─────────────┬ Representation ──────────┐
│ 見ている値 / 短いコード        │ 2進数 / 16進数 / 文字     │
│                              │                           │
├ CPU / Bus / Memory ──────────┼ Explain / Quiz ───────────┤
│ CPU (MAR, MDR)               │ 解説 / キーガイド / 問題   │
│ Address Bus / Data Bus       │                           │
│ Memory セル                   │                           │
└───────────────────────────────────────────────────────────┘
 [n]next [p]prev [r]reset [q]quit [h]hint [space]auto [1/2/3]mode
```

### 比率（原資料 L4953–L4968）

- **縦方向**: 上段 35% / 下段 65%（下段の情報量が多いため）
- **横方向 上段**: 左50% / 右50%
- **横方向 下段**: 左60% / 右40%

### 推奨・最小サイズ

- 推奨: 120×40 以上
- 最小: 80×24（この時点でレイアウトが崩れない保証は MVP で検証）
- サイズ取得不能時は 80×24 に fallback

## 4ペインの確定仕様

| ペイン | 表示名 | 内容 |
|--------|-------|------|
| **TargetPane** | `Target / Source` | 見ている値（数値・文字）または短いCコード |
| **RepresentationPane** | `Representation` | 同じ値の別表現（10進・2進・16進・Char） |
| **CpuBusMemoryPane** | `CPU / Bus / Memory` | MAR/MDR・Address Bus・Data Bus・Memory セルの状態遷移 |
| **ExplainQuizPane** | `Explain / Quiz` | 解説文・キーガイド・クイズ |

## 操作キー（確定 / 9キー上限）

原資料 L4629–L4641, L4641 の「9キー以上は脱落リスクのため使わない」制約に従う。

| キー | 動作 |
|-----|------|
| `n` | 次のステップへ |
| `p` | 前のステップへ戻る |
| `r` | レッスン先頭に戻る |
| `q` | 終了 |
| `h` | ヒント表示（クイズ補助） |
| `space` | 自動再生 / 停止 |
| `1` / `2` / `3` | 表示モード切替（2進/16進/文字） |

操作キーのリマップは **MVP では不要**（未決）。

## 色分け（確定 / 原資料 L5078–L5085）

| 対象 | 色 |
|-----|----|
| CPU | 黄色 |
| Address Bus | 青 |
| Data Bus | 緑 |
| Selected Memory | 反転表示 |
| Quiz 正解 | 緑 |
| Quiz 不正解 | 赤 |

ANSI 24bit カラーは使わず、8色の標準カラーで表現する（ターミナル差を避ける）。

## TypeScript 型定義（確定）

原資料 L4879–L4950 の型をそのまま採用する。

```ts
// ── ペイン状態 ──
type TargetPaneState = {
  title: string;
  lines: string[];
  highlightLine?: number;
};

type RepresentationItem = {
  label: string;     // "Decimal" / "Binary" / "Hex" / "Char"
  value: string;     // "65" / "01000001" / "0x41" / "'A'"
};

type RepresentationPaneState = {
  items: RepresentationItem[];
};

type MemoryCell = {
  address: string;   // "0x1000"
  value: string;     // "0x41"
  label?: string;    // "<x>" のような注記
  selected?: boolean;
};

type CpuBusMemoryState = {
  mar?: string;                // "0x1000"
  mdr?: string;                // "0x41"
  addressBusValue?: string;
  dataBusValue?: string;
  memoryCells: MemoryCell[];
  phase: "idle" | "send-address" | "select-memory" | "return-data" | "done";
};

type QuizChoice = {
  id: string;        // "a" / "b" / "c"
  text: string;
};

type Quiz = {
  question: string;
  choices: QuizChoice[];
  selectedId?: string;
  correctId: string;
  revealed?: boolean;
};

type ExplainQuizPaneState = {
  explanationLines: string[];
  keyHints: string[];
  quiz?: Quiz;
};

// ── レッスン構造 ──
type LessonStep = {
  target: TargetPaneState;
  representation: RepresentationPaneState;
  cpuBusMemory: CpuBusMemoryState;
  explainQuiz: ExplainQuizPaneState;
};

type Lesson = {
  id: string;        // "L0-1"
  title: string;     // "2進数とビット"
  steps: LessonStep[];
};

// ── アプリ全体 ──
type DisplayMode = "binary" | "hex" | "char";

type AppState = {
  lesson: Lesson;
  stepIndex: number;
  displayMode: DisplayMode;     // 1/2/3キーで切替
  autoPlay: boolean;
  quizInput: string | null;
  quit: boolean;
};

type KeyEvent =
  | { kind: "next" }
  | { kind: "prev" }
  | { kind: "reset" }
  | { kind: "quit" }
  | { kind: "hint" }
  | { kind: "toggleAuto" }
  | { kind: "mode"; mode: DisplayMode }
  | { kind: "choice"; id: string };
```

## 表記規則

- **アドレス**: `0x1000` 形式（大文字 `X` は使わない）
- **バイト値**: `0x41` 形式
- **2進数**: `01000001` — 8bit ゼロ埋め、桁区切りなし
- **10進数**: `65` — そのまま
- **文字**: `'A'` — シングルクォートで囲む

## 責務分離

| モジュール | 責務 |
|-----------|-----|
| `src/content/` | `Lesson` データの静的定義（L0-1 / L0-2 / L0-3） |
| `src/state/` | `AppState` の純粋関数遷移（`reduce(state, event)`） |
| `src/render/` | `AppState` → ターミナル出力。4ペインレンダラー |
| `src/input/` | `process.stdin` を raw モードで読み `KeyEvent` を生成 |
| `src/app.ts` | エントリ。input → state → render のループ |

純粋関数で状態遷移を切り出しておくことで、render/input を差し替えて移植・テストしやすくする。

## 実装優先順（原資料 L5105–L5115）

1. 固定 4ペインレイアウトの描画
2. ステップ送り（`n`/`p`）と `stepIndex` 遷移
3. `CpuBusMemoryPane` の強調表示（選択セル反転・バス色分け）
4. `Quiz` 表示と判定
5. `RepresentationPane` の表示モード切替（`1`/`2`/`3`）

L0-1 で 1〜4 が必要、5 は L0-1 完成後に追加でよい。

## macOS / WSL 移植の観点

| 項目 | 方針 |
|------|-----|
| ターミナルサイズ | `process.stdout.columns/rows` / 未取得時 80×24 |
| ANSI カラー | 8色標準カラーのみ使用 |
| 日本語幅 | 上段ペインでは日本語禁止（英数記号のみ）。解説文のみ日本語 |
| キー入力 | `process.stdin.setRawMode(true)` + `data` イベント |
| 改行 | 出力は `\n` 固定 |
| 文字コード | UTF-8 固定 |

## TUI ライブラリ選定（Phase 2 冒頭で決定）

| 候補 | 判断基準 |
|------|---------|
| **自前 ANSI** | 依存ゼロ・挙動が読みやすい。30分スパイクで 4ペイン描画の当たりを付ける |
| **ink** | React 風。依存が重い。自前が厳しい場合のフォールバック |
| **blessed** | 枠描画が楽だがメンテ状況が懸念 |

**初期方針**: 自前 ANSI で始める。

## 未決事項

| 項目 | 扱い |
|------|-----|
| 各ステップの Explanation 文（L0-1〜L0-3）| `04-l0-curriculum-spec.md` で暫定文を書き、L0-1 実装後にユーザーが調整 |
| クイズ難度レベル（初級/中級） | MVP では単一難度 |
| 最小画面サイズでのレイアウト崩れ検証 | Phase 2 で実機確認 |
| キー操作のカスタマイズ | MVP では固定 |
| 学習進捗の永続化 | MVP では不要。将来 `~/.ai-to-web/progress.json` に保存 |
| 自動再生（space）の速度 | デフォルト 1.5 秒/ステップ。設定は MVP で見送り |

## このあとの作業

1. `04-l0-curriculum-spec.md`（L0-1/L0-2/L0-3 の詳細仕様）に着手
2. 04 が書けた段階で、本書の型に L0-1 のデータを流し込めるか確認
3. Phase 2 冒頭で TUI ライブラリを決定しスパイク
4. `src/` 雛形と L0-1 スライスの実装

---

> この文書は Claude Opus 4.7 が生成しました。内容の正確性は人間のレビューを経て確認してください。
>
> Claude Opus 4.7 — Anthropic
