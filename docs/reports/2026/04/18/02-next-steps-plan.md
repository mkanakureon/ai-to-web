---
generated_by: Claude Opus 4.7
date: 2026-04-18
type: plan
project: ai-to-web
---

# 次工程 実行計画書 — カリキュラム仕様・TUI設計・L0-1スライス

> Generated by Claude Opus 4.7 — 2026年4月18日

## 背景

`01-learning-tool-source-analysis-report.md` で学習ツールの設計方針4点（C は観察対象・一貫メンタルモデル・CLI/TUI・AI対話のための構造化思考）が確定した。ここから実装に向けて **3つの成果物** を順に作る必要がある:

1. カリキュラム仕様書（L0 導入 3 レッスン + L1–L10 の到達目標）
2. TUI 4ペインの TypeScript 型設計
3. L0-1（2進数 → ASCII）の最小動作スライス実装

本計画書は、この 3 つの **順序・依存関係・受け入れ基準** を固定し、実装の迷走を防ぐためのもの。

## ゴール

「L0-1 レッスンが macOS のコンソール上で動き、2進数 65 → 0x41 → 'A' の変換が 4 ペインで観察できる」状態に到達する。WSL 移植はその後。

## スコープ

### 成果物 A: カリキュラム仕様書

- **ファイル名（予定）**: `XX-curriculum-spec.md`
- **入力**: `01-learning-tool-source-analysis-report.md` / 原資料 L3339–L4080, L4593–L5131
- **内容**:
  - L0-1 / L0-2 / L0-3 の詳細（到達目標・扱う概念・観察対象・終了時にできること）
  - L1–L10 は **到達目標の一行要約のみ**（詳細は L0 完成後）
  - レッスン間の依存グラフ
- **受け入れ基準**:
  - L0 の各レッスンに「到達目標」「扱う概念」「観察対象」「終了時にできること」の 4 項目が明記
  - L1–L10 は順序と前提レッスンが示されている
  - 一貫メンタルモデル「CPU がアドレスを指定してメモリからデータを読む」が全レッスンで使い回されていることが確認できる

### 成果物 B: TUI 4ペイン型設計書

- **ファイル名**: `03-tui-foundation-design.md`（本計画書と同時に着手）
- **入力**: 原資料 L4593–L5131（4ペインの具体化）/ 01 レポート
- **内容**:
  - 画面レイアウト（4ペインの配置・サイズ）
  - TypeScript 型（`Lesson`, `Step`, `AppState`, `PaneProps` など）
  - レンダリング抽象と入力処理の責務
  - macOS / WSL で壊れやすい箇所の整理
- **受け入れ基準**:
  - L0-1 のデータが型に収まることが、サンプルデータで確認できる
  - 型の責務が明確で、実装時に追加で設計判断が必要な箇所は「未決事項」として列挙されている

### 成果物 C: L0-1 最小動作スライス

- **対象ディレクトリ**: `src/`（新規作成）
- **入力**: A の L0-1 部分 + B の型設計
- **内容**:
  - 4 ペイン固定レイアウトの最小描画
  - ステップ送り（キー入力で次のステップへ）
  - 65 → 0x41 → 'A' の変換プロセスを順に見せる
  - 単純なクイズ（最後に 1 問）
- **受け入れ基準**:
  - `npm run dev` で起動する
  - キー操作でステップが進む
  - 4 ペインに想定どおりの内容が出る
  - **macOS で動作確認済み**（WSL 検証は Phase 3 に後送）

## 工程（フェーズ分割）

### Phase 1: カリキュラム L0 確定 + TUI 型設計（並行可）

| トラック | 作業 | 依存 |
|---------|------|-----|
| トラック1 | 成果物 A のうち **L0 の 3 レッスン分だけ** を仕様化 | 01 レポート |
| トラック2 | 成果物 B（TUI 型設計）を作成 | 原資料 L4593–L5131 を別途精読 |

**完了条件**: L0-1 のデータを型 B に流し込んだときに矛盾がないことが示せる。

### Phase 2: L0-1 スライス実装

- `package.json` / `tsconfig.json` / `src/` の雛形作成
- B の型に沿って L0-1 を 1 本だけ実装
- TUI ライブラリの選定（候補: `ink` / `blessed` / 自前 ANSI）を Phase 2 冒頭で決定

**完了条件**: 上記「成果物 C の受け入れ基準」全項目を満たす。

### Phase 3: L1–L10 の仕様化 + WSL 移植検証

- 成果物 A の残り（L1–L10 詳細）を書く
- L0-1 を WSL 上で動作確認し、プラットフォーム差のバグを潰す
- カリキュラム全体での依存グラフを再点検

## 依存関係

```
01 report ──┬─> A (L0 spec) ─┐
            │                 ├─> C (L0-1 slice) ─> WSL検証
            └─> B (TUI型)   ─┘
```

- A と B は **並行可**。B は原資料の画面仕様箇所を別途精読する必要あり
- C は A（L0-1 部分）と B の両方に依存
- A の L1–L10 詳細は C の後でよい

## リスク

| リスク | 影響 | 対策 |
|-------|------|-----|
| 原資料 5131 行の中に見落としがある | 設計のやり直し | B 着手前に L4593–L5131 を Explore エージェントで再精読 |
| TUI ライブラリ選定で詰まる | Phase 2 遅延 | Phase 2 初日に最大 3 候補で 30 分スパイクして決定 |
| macOS / WSL の ANSI 差異 | WSL で表示崩れ | CLAUDE.md の Forbidden Patterns を守る / Phase 3 で検証 |
| L0 の説明粒度が細かすぎ / 粗すぎ | 教材として機能しない | L0-1 実装後、ユーザー自身で動かして粒度調整 |
| 教材コンテンツが無限に膨らむ | スコープクリープ | L1–L10 の詳細は Phase 3 まで凍結 |

## 除外（この計画では扱わない）

- L1–L10 の詳細仕様（Phase 3 まで待つ）
- テキスト RPG との共通部品化（原資料 L2213–L2442 で「狙わない」と確定済み）
- Web / サーバー / DB の実装（学習ツール自体は CLI 完結）
- CI/CD、デプロイ
- 自動テストの整備（L0-1 が動いてから検討）

## 次のアクション（直近）

1. **本計画書のレビュー**（ユーザー確認）
2. `03-tui-foundation-design.md` を本計画書と同時に提出 — 同時に読むと Phase 1 の進め方が具体化する
3. 原資料 L4593–L5131 を Explore で再精読（B 本書きの前）
4. Phase 1 トラック1: L0-1 の仕様化から着手

---

> この文書は Claude Opus 4.7 が生成しました。内容の正確性は人間のレビューを経て確認してください。
>
> Claude Opus 4.7 — Anthropic
