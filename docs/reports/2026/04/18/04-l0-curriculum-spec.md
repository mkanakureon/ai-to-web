---
generated_by: Claude Opus 4.7
date: 2026-04-18
type: spec
project: ai-to-web
---

# L0 導入カリキュラム 仕様書

> Generated by Claude Opus 4.7 — 2026年4月18日

## 背景

`02-next-steps-plan.md` Phase 1 トラック1。`03-tui-foundation-design.md` の TUI 基盤上で動く、L0 導入 3 レッスンの **画面ごとの状態** を確定させる。

実装者がこの仕様書から `content/` ディレクトリ下の `Lesson` データを **そのまま書き起こせる**粒度で記述する。

## L0 全体の到達目標

**「CPU がアドレスを指定してメモリからデータを読む」という 1 つのメンタルモデルを、同じ 4ペイン骨格で 3 回見せる。**

- L0-1: バイト単位の値が複数の表現を持つことを知る
- L0-2: CPU・バス・メモリの役割と読み取り手順を知る
- L0-3: C のソースコードが L0-1 と L0-2 の組み合わせで動いていることを知る

## 共通仕様

- 各レッスン **5 ステップ**
- 操作: `n` 次 / `p` 戻る / `r` リセット / `q` 終了 / `h` ヒント / `space` 自動 / `1`-`3` 表示モード
- 凡例の色（CPU=黄 / AddrBus=青 / DataBus=緑 / 選択セル=反転）
- 進行中ずっと `keyHints: ["[n]next", "[p]prev", "[q]quit"]` を最下部に表示

---

## L0-1: 2進数とビット

> 原資料 L4347–L4720

**到達目標**: 10進数 `65` が 2進数 `01000001` / 16進数 `0x41` / 文字 `'A'` と同じものだと理解する。

**終了時にできること**:
- ASCII の `'A'` が 10 進で何番か即答できる
- 1 バイト = 8 ビットだと即答できる
- 2 進数と 16 進数が同じ値の別表記だと説明できる

### ステップ 1 — 10進数を見る

| ペイン | 内容 |
|-------|-----|
| Target | `title: "Value"` / `lines: ["Value: 65"]` |
| Representation | `[{label:"Decimal", value:"65"}]` |
| CpuBusMemory | `memoryCells: [{address:"0x1000", value:"0x41"}]` / `phase:"idle"` |
| Explain | `"これから 65 という 1 バイトの値を観察します。"` / `"同じ値が複数の顔を持ちます。"` |

### ステップ 2 — 2進数を並べる

| ペイン | 差分 |
|-------|-----|
| Representation | `+ {label:"Binary", value:"01000001"}` |
| Explain | `"これが 2 進数の姿。8 個の 0/1 が 1 バイトです。"` |

### ステップ 3 — ビット位置を示す

| ペイン | 差分 |
|-------|-----|
| Target | `lines: ["Value: 65", "Bit:  7 6 5 4 3 2 1 0", "Val:  0 1 0 0 0 0 0 1"]` / `highlightLine: 2` |
| Explain | `"右端がビット 0（1 の位）、左端がビット 7。各桁の重みは 2 の累乗です。"` |

### ステップ 4 — 16進数を並べる

| ペイン | 差分 |
|-------|-----|
| Representation | `+ {label:"Hex", value:"0x41"}` |
| Explain | `"4 ビットずつまとめると 16 進数になります。0100 → 4、0001 → 1、合わせて 0x41。"` |

### ステップ 5 — 文字の意味を示す

| ペイン | 差分 |
|-------|-----|
| Representation | `+ {label:"Char", value:"'A'"}` |
| CpuBusMemory | `memoryCells[0].label: "'A'"` / `memoryCells[0].selected: true` |
| Explain | `"ASCII コード表で 65 は 'A'。同じ 1 バイトが 10/2/16 進と文字、4 つの顔を持ちます。"` |
| Quiz | 下記 |

**Quiz（ステップ 5 で出題）**:

```
Q: 1 バイトは何ビットですか?
   a) 4
   b) 8
   c) 16
正解: b
```

(原資料 L4706–L4720 のクイズ例より)

---

## L0-2: CPU / メモリ / バス

> 原資料 L4722–L4800

**到達目標**: CPU が **MAR に番地を置き、Address Bus でメモリに伝え、Data Bus で値を受け取り、MDR に入れる**、という読み取り 1 サイクルを再現できる。

**終了時にできること**:
- MAR / MDR が何を指すか説明できる
- Address Bus と Data Bus の向きを説明できる
- 「CPU が先にアドレスを出す」と答えられる

### ステップ 1 — ゴール提示

| ペイン | 内容 |
|-------|-----|
| Target | `title: "Goal"` / `lines: ["read memory at 0x1000"]` |
| Representation | `[{label:"Address", value:"0x1000"}]` |
| CpuBusMemory | `memoryCells: [{address:"0x0FFC", value:"0x10"}, {address:"0x1000", value:"0x41"}, {address:"0x1004", value:"0x42"}]` / `phase:"idle"` |
| Explain | `"アドレス 0x1000 の値を読みに行きます。登場するのは CPU、バス、メモリの 3 つ。"` |

### ステップ 2 — CPU が MAR にアドレスを入れる

| ペイン | 差分 |
|-------|-----|
| CpuBusMemory | `mar: "0x1000"` / `addressBusValue: "0x1000"` / `phase: "send-address"` |
| Explain | `"CPU は MAR (Memory Address Register) に読みたい番地を置きます。MAR = 0x1000。"` |

### ステップ 3 — メモリセルが選ばれる

| ペイン | 差分 |
|-------|-----|
| CpuBusMemory | `memoryCells[1].selected: true` / `phase: "select-memory"` |
| Representation | `+ {label:"Data", value:"0x41"}` |
| Explain | `"Address Bus で届いた番地をメモリがデコードし、0x1000 のセルが選ばれます。"` |

### ステップ 4 — Data Bus で値が返る

| ペイン | 差分 |
|-------|-----|
| CpuBusMemory | `dataBusValue: "0x41"` / `phase: "return-data"` |
| Explain | `"選ばれたセルは Data Bus に値を載せて CPU に返します。向きは メモリ → CPU。"` |

### ステップ 5 — MDR で受け取る

| ペイン | 差分 |
|-------|-----|
| CpuBusMemory | `mdr: "0x41"` / `phase: "done"` |
| Representation | `+ {label:"Char", value:"'A'"}` |
| Explain | `"CPU は MDR (Memory Data Register) に値を受け取ります。MDR = 0x41 = 'A'。読み取り 1 回の完了です。"` |
| Quiz | 下記 |

**Quiz（ステップ 5 で出題）**:

```
Q: メモリ読み取りで、CPU が最初に出すのはどれですか?
   a) Data Bus に値を流す
   b) Address Bus にアドレスを流す
   c) MDR に値を受け取る
正解: b
```

(原資料 L4794–L4798 より)

---

## L0-3: メモリアクセスと C の接続

> 原資料 L4802–L4864

**到達目標**: C の `char x = 'A'; print(x);` が、L0-1 + L0-2 を合成した動作として読める。

**終了時にできること**:
- `x` は名前にすぎず、実体はメモリ上の 1 バイトだと説明できる
- C のコードを見たとき、対応するメモリとバスの動きを想像できる

### ステップ 1 — ソースを見る

| ペイン | 内容 |
|-------|-----|
| Target | `title: "C Source"` / `lines: ["char x = 'A';", "print(x);"]` |
| Representation | `[{label:"Source", value:"char x"}]` |
| CpuBusMemory | `memoryCells: [{address:"0x0FFC", value:"0x10"}, {address:"0x1000", value:"0x41"}, {address:"0x1004", value:"0x42"}]` / `phase:"idle"` |
| Explain | `"C のコードを 1 バイトレベルで追いかけます。"` |

### ステップ 2 — x の場所を示す

| ペイン | 差分 |
|-------|-----|
| Representation | `+ {label:"x address", value:"0x1000"}` / `+ {label:"x value", value:"0x41"}` |
| CpuBusMemory | `memoryCells[1].label: "<x>"` |
| Explain | `"x は名前です。実体はメモリの 0x1000 番地にある 1 バイト 0x41。"` |

### ステップ 3 — print(x) が CPU に読み取りを頼む

| ペイン | 差分 |
|-------|-----|
| Target | `highlightLine: 1` (`print(x);` 側) |
| CpuBusMemory | `mar: "0x1000"` / `addressBusValue: "0x1000"` / `phase:"send-address"` |
| Explain | `"print(x) は x を渡すために、まず中身を読みます。L0-2 と同じ手順。"` |

### ステップ 4 — Data Bus で値が返り MDR に入る

| ペイン | 差分 |
|-------|-----|
| CpuBusMemory | `memoryCells[1].selected:true` / `dataBusValue:"0x41"` / `mdr:"0x41"` / `phase:"done"` |
| Explain | `"CPU は 0x41 を受け取りました。この値は ASCII で 'A'。"` |

### ステップ 5 — 出力される

| ペイン | 差分 |
|-------|-----|
| Target | `lines: ["char x = 'A';", "print(x);", "", "Output: A"]` |
| Representation | `+ {label:"Char", value:"'A'"}` |
| Explain | `"x は名前、実体はメモリ上の 0x41、読むときは CPU がアドレスを指定する。C の 2 行はこの一連の動きです。"` |
| Quiz | 下記 |

**Quiz（ステップ 5 で出題）**:

```
Q: C の変数 x が `char x = 'A'` で作られたとき、x の正体は次のどれに最も近いですか?
   a) 'A' という文字そのもの
   b) メモリ上のある番地に置かれた 1 バイト (0x41)
   c) CPU の中のレジスタに永続的に置かれた値
正解: b
```

(原資料 L4854–L4862 の言い切り文に対応)

---

## L1–L10（到達目標のみ / 詳細は Phase 3）

> 原資料 L4086–L4265

| ID | 題 | 到達目標（一行） |
|----|-----|----------------|
| L1 | 整数の基本 | `uint8_t` / `uint16_t` / `uint32_t` のバイト幅を理解する |
| L2 | エンディアン | Little / Big Endian のバイト順を説明できる |
| L3 | char と文字列 | ASCII と NULL 終端文字列の構造を読める |
| L4 | 配列とポインタ | 配列アクセスがアドレス計算であると理解する |
| L5 | ポインタの正体 | ポインタ = アドレスを保持する変数だと言える |
| L6 | struct とメモリ配置 | struct がメモリ上でどう並ぶか読める |
| L7 | パディング | alignment のため挿入される見えない領域を説明できる |
| L8 | 文字列とバイト列 | 同じバイト列が文字列でもバイナリでもある場面を理解する |
| L9 | HTTP の正体 | HTTP がテキストに見えるバイト列プロトコルだと知る |
| L10 | 全統合 | C → HTTP → TS の階層を一本で説明できる |

依存関係: L1 → L2 → (L3 / L4) → L5 → L6 → L7 → L8 → L9 → L10。L0 3 レッスンは全 L1–L10 の前提。

## スコープ外（L0 では扱わない）

原資料 L3821–L3828, L4523–L4532, L4641 より:

- 複雑なポインタ（ポインタのポインタ等）
- マクロ、ビットフィールド、未定義動作の深掘り
- キャッシュ階層、NUMA、仮想メモリ
- 9 キーを超える操作
- RPG 共通化（原資料 L2213–L2442 で確定）

## 未決事項（実装後に調整）

- 各ステップの Explanation 文は暫定。L0-1 実装後にユーザーが語感を調整
- ヒント (`h` キー) の内容は各ステップの Explanation の再掲 + 具体例で MVP は十分
- クイズ不正解時の挙動: 正解を表示して次へ進めるか、もう一度挑戦か（MVP では「正解表示して進める」）
- L0-2 の `memoryCells` に 3 個以外（5 個など）を入れるか — 原資料 L5064–L5072 で「3–5 個」とあるが、L0-2/L0-3 では 3 個に統一

## このあとの作業

1. 本書の L0-1 データが `03-tui-foundation-design.md` の型に収まるかを、サンプルコード（実装なし）で確認
2. TUI ライブラリ選定スパイク（Phase 2 冒頭）
3. `src/` 雛形 → L0-1 スライス実装

---

> この文書は Claude Opus 4.7 が生成しました。内容の正確性は人間のレビューを経て確認してください。
>
> Claude Opus 4.7 — Anthropic
